
const Authentication = require('./authentication');
const WebServer = require('./webserver');
const SocketServer = require('./socketserver');

module.exports = { Authentication, WebServer, SocketServer };
